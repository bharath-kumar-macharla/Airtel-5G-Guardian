# Airtel 5G Guardian — Python dependencies
# Install with: pip install -r requirements.txt

# ── Runtime ───────────────────────────────────────────────────────────────────
customtkinter>=5.2.2
plyer>=2.1.0
pystray>=0.19.5
Pillow>=10.0.0

# ── Build / Packaging (developer only — not needed to run the app) ────────────
# pyinstaller>=6.0.0
